curl -s -X POST -d '{ "query": "mutation SaveUrl($input: SaveUrlInput!) { saveUrl(input: $input) { ... on SaveSuccess { url clientRequestId } ... on SaveError { errorCodes message } } }", "variables": { "input": { "clientRequestId": "'$(uuidgen)'", "source": "api", "url": "'"$1"'" }} }' -H 'content-type: application/json' -H 'authorization: '"$(cat ~/.local/share/omnivore)"'' https://api-prod.omnivore.app/api/graphql
